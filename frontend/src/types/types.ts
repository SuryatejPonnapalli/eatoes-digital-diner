export interface MenuData {
  id: any;
  itemName: string;
  cost: number;
  category: string;
  image: string;
  desc: string;
}

export interface CartType {
  itemName: string;
  cost: number;
  quantity: number;
}
